[Unit]
Description=IMP2 robot main bringup
After=network.target docker.service
Requires=docker.service

[Service]
Type=simple
User=imp2
WorkingDirectory=/opt/imp2
ExecStart=/usr/bin/docker compose -f /opt/imp2/docker-compose.yml up
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
